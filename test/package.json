{
  "name": "finally-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "FinAlly end-to-end Playwright suite (runs inside docker-compose.test.yml).",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.60.0"
  }
}
